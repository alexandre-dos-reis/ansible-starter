all:
  children:

    test:
      hosts:

        test-build:
          ansible_host: 172.16.0.11
          ansible_port: 22
          provider_user: debian
          provider_password: password

        test-app:
          ansible_host: 172.16.0.12
          ansible_port: 22
          provider_user: debian
          provider_password: password
      
    prod:
      hosts:
        adr-build:
        adr-app:

    app:
      hosts:
        test-app:
        adr-app:
    
    build:
      hosts:
        test-build:
        adr-build: